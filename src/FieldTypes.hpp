#pragma once

enum class FieldType {
        TEXT,
        NUMBER,
        DATE,
        TIME,
        DATETIME,
        BOOLEAN,
        BLOB
};

enum class HtmlInputType {
        TEXT,
        NUMBER,
        DATE,
        TIME,
        DATETIME,
        CHECKBOX,
        RADIO,
        SELECT,
        TEXTAREA
};